{ config, pkgs, lib, ... }:
{
  # ============================================================================
  # BROWSER CONFIGURATION
  # ============================================================================
  # This file can be used for browser-specific configurations
  # Currently handled in privacy.nix
  # ============================================================================
}